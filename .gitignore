# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables — never commit real credentials.
# `.env.example` is intentionally NOT ignored (it documents the required keys).
.env
.env.*
!.env.example

# Firebase service accounts / admin credentials (must never be committed)
*serviceAccount*.json
*firebase-adminsdk*.json
firebase-debug.log

# Dependencies
node_modules

# Build output
.next/
out/
build/

# Misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
